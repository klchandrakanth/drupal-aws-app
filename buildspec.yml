version: 0.2

phases:
  pre_build:
    commands:
      - echo "Pre-build phase"
      - aws --version
      - echo "ECR login step"
      - echo "Repository URI: 396503876336.dkr.ecr.us-east-1.amazonaws.com/production-drupal"
      - echo "Image tag: latest"
  build:
    commands:
      - echo "Build phase"
  post_build:
    commands:
      - echo "Post build phase"